#### ❓ Needs Human Review — `ArgumentNullException` body lists "cannot be serialized" as a null condition (26 sites)

**Author (opus):** 26 sites (e.g. [#L56](src/Data.Interfaces/Collections/IReliableDictionary.cs#L56), [#L73](src/Data.Interfaces/Collections/IReliableDictionary.cs#L73), [#L90](src/Data.Interfaces/Collections/IReliableDictionary.cs#L90), [#L191](src/Data.Interfaces/Collections/IReliableDictionary.cs#L191), [#L355](src/Data.Interfaces/Collections/IReliableDictionary.cs#L355)) carry "...or `key` is `null` or cannot be serialized..." inside the `ArgumentNullException` element. Per the [.NET Framework Design Guidelines](https://learn.microsoft.com/dotnet/standard/design-guidelines/), `ArgumentNullException` is for null arguments only; a serialization failure on a non-null key is conventionally a different exception type. The actual runtime type cannot be determined from this repo — runtime verification needed.

**Cross-check (gpt): Agree.** Confirmed 26 sites; a non-null key that cannot be serialized is not a null-argument condition, and exception docs should describe the actual throwing condition. Did not find evidence that serialization failure surfaces as `ArgumentNullException`.

> Both models agree the current wording is questionable, but neither can determine the correct exception type without runtime behavior — the removed todo itself noted this and allowed for "record the verification and leave unchanged" if it turns out to be `ArgumentNullException`. A human must confirm the actual exception type before changing or keeping the text.

#### ❓ Needs Human Review — `ClearAsync` omits `<exception cref="FabricObjectClosedException">`

**Author (opus):** [#L184](src/Data.Interfaces/Collections/IReliableDictionary.cs#L184): `ClearAsync(TimeSpan, CancellationToken)` documents `ArgumentException`, `TimeoutException`, `OperationCanceledException`, and `FabricNotPrimaryException`, but not `FabricObjectClosedException` — the sole state-touching member omitting it. Could not confirm from the reference: `DistributedDictionary.ClearAsync` calls `ThrowIfNotRegistered()` then delegates to `dataStore.ClearAsync`, and its own XML doc omits `FabricObjectClosedException`. Runtime verification needed.

**Cross-check (gpt): Abstain.** The omission and inconsistency are real, but `DistributedDictionary.ClearAsync` calls `ThrowIfNotRegistered()` (which throws `InvalidOperationException`) then delegates; its own comments also omit `FabricObjectClosedException`. Evidence supports "inconsistent and worth domain confirmation," not enough to assert the exception is accurate.

> No model could verify the closed/deleted path actually surfaces `FabricObjectClosedException`. A human with runtime knowledge should decide whether to add the standard element or document why `ClearAsync` is exempt.

#### ❓ Needs Human Review — factory-null `ArgumentNullException` docs not evidenced by the implementation path

**Author (gpt):** Several overloads document `ArgumentNullException` for null factory delegates: `addValueFactory`/`updateValueFactory` at [#L90](src/Data.Interfaces/Collections/IReliableDictionary.cs#L90), [#L110](src/Data.Interfaces/Collections/IReliableDictionary.cs#L110), [#L132](src/Data.Interfaces/Collections/IReliableDictionary.cs#L132), [#L158](src/Data.Interfaces/Collections/IReliableDictionary.cs#L158), and `valueFactory` at [#L358](src/Data.Interfaces/Collections/IReliableDictionary.cs#L358) and [#L390](src/Data.Interfaces/Collections/IReliableDictionary.cs#L390). In the reference implementation, those delegates are captured inside wrapper lambdas and dereferenced later via `.Invoke(...)`; there is no up-front validation before passing the wrapper to TStore, so a null delegate appears to fail only when actually invoked. Suggested change: verify runtime behavior and revise the exception clauses, or add a scoped TODO noting the documented `ArgumentNullException` needs runtime/API-owner confirmation. Do not "fix" by adding validation (out of scope).

**Cross-check (opus): Agree.** Verified `DistributedDictionary.AddOrUpdateAsync` and `GetOrAddAsync` cores: no up-front null validation of the factories; the only `Requires.ThrowIfNull` calls guard `txn`/`replicatorTxn`. A null factory would surface as `NullReferenceException` at invocation time (for `AddOrUpdateAsync`, only when the relevant branch is taken), not an up-front `ArgumentNullException`. Caveat: only the `DistributedDictionary` layer is visible; validation in an intervening public surface cannot be ruled out — but the finding correctly scopes its conclusion to "not evidenced; needs confirmation," and the cautious disposition (verify or scoped TODO, no validation) matches the precedent in the existing needs-human-review file.

> The reference implementation is not present in this repo, so the documented `ArgumentNullException` for null factory delegates cannot be verified against the API surface. A human with runtime knowledge must confirm whether a null factory surfaces as an up-front `ArgumentNullException`; if it instead surfaces as `NullReferenceException` at invocation time, the exception clauses should be revised. Do not add validation.