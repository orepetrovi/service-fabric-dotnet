#### ❓ Needs Human Review — `OperationCanceledException` on `RestoreAsync(string, RestorePolicy, CancellationToken)`

**Reported by Gemini (⚠️, narrowed from an all-four-method proposal after cross-check); GPT → Agree on the narrowed scope; Opus → Reject on double-check.** Escalated because the two non-authoring models split 1-1.

> **Gemini (author, narrowed insistence):** Retract for `OpenAsync`/`ChangeRoleAsync`/`CloseAsync` (no deterministic cancellation evidence). **Insist** for `RestoreAsync(string, RestorePolicy, CancellationToken)` only: the reference restore path calls `BackupFolderInfo.AnalyzeAsync(cancellationToken)`, which actively calls `ThrowIfCancellationRequested`, deterministically throwing `OperationCanceledException`.
>
> **GPT (double-check): Agree.** The reference restore overload passes the caller's token into `AnalyzeAsync(cancellationToken)`, whose backup metadata/log readers call `cancellationToken.ThrowIfCancellationRequested()`, and also into `stateManager.RestoreAsync(...)`. That makes cancellation a realistic user-facing nested exception for this overload. Commit `2cc651d4` addressed "documents no exceptions" and added `ArgumentException` + `InvalidOperationException`; its message does not reject `OperationCanceledException` and its diff does not remove it — it narrowed that finding, not the method's exception set forever. Scope must stay narrowed: do not add to `RestoreAsync(string)` (delegates with `CancellationToken.None`) or to `OpenAsync`/`ChangeRoleAsync`/`CloseAsync`.
>
> **Opus (double-check): Reject.** The throw chain is real (so "is it thrown" is satisfied), but the rule's qualifier for nested-member exceptions is "most likely to encounter," not "can be thrown." In the *same* reference file the author documents `OperationCanceledException` on `BackupAsync` (L301) yet documents four exceptions on `RestoreAsync` (L676-679) and omits `OperationCanceledException`, despite passing the token into the same throwing chain — an intentional "most likely to encounter" judgment (restore runs only inside data-loss recovery; cancellation during the brief folder-analysis window is a corner case). The workspace file already mirrors this exact asymmetry. Separately, commit `2cc651d4` deliberately pruned `RestoreAsync` to two contract-level exceptions and dropped others (including `InvalidDataException`, which originates from the *same* `BackupMetadataFile.OpenAsync` a few lines above the `ThrowIfCancellationRequested` calls); adding `OperationCanceledException` now pulls a third exception out of the very `AnalyzeAsync` chain the prior finding pruned, reopening an addressed decision the request forbids.

**Human decision needed:** Both sides agree the exception *can* be thrown. The disagreement is whether documenting it (a) satisfies the "most likely to encounter" bar for nested exceptions and (b) contradicts the deliberate `2cc651d4` narrowing of `RestoreAsync`'s exception set. If you decide to proceed, the proposed addition (timeout-style overload only) is:
```xml
/// <exception cref="OperationCanceledException">The operation was canceled via <paramref name="cancellationToken"/>.</exception>
```